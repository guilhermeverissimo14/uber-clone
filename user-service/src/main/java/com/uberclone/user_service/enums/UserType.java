package com.uberclone.user_service.enums;

// Enum para representar os tipos de usuários no sistema
public enum UserType {
    // Passageiro comum    
    PASSENGER,
    // Motorista
    DRIVER,
    // Ambos (pode ser passageiro e motorista)
    BOTH
}
